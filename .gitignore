# oh-my-codex / oh-my-x
.omc/
.omx/
.omo/

# User customizations
custom/
.patina.yaml
.patina/aside.json
.patina/.aside-*
.patina/.aside.lock
.patina-aside-output-*/

# OS / editor
.DS_Store
*.swp
*~

# Ops / runtime logs
node_modules
artifacts/hf-export/
.env
.gjc/
plans/
__pycache__/
.pytest_cache/

# secrets / credentials
.env.*
*.key
*.pem
secrets/
credentials/
.aws/
.ssh/

# OpenClaw / Claude Code workspace files (runtime-specific, not part of the skill)
.openclaw/
.workclaw/
.claude/
# The root development entrypoint is public; scoped agent files stay ignored.
AGENTS.md
!/AGENTS.md
BOOTSTRAP.md
CLAUDE.md
IDENTITY.md
USER.md
MEMORY.md
HEARTBEAT.md
SOUL.md
TOOLS.md
memory/

# Maintainer/agent-internal docs (runbooks, backlogs, launch playbooks) live in
# the private maintainer workspace (.omc/research/), never in the public tree.
docs/internal/

# Marketing bot workspace template (internal ops)

# Generated benchmark artifacts (regenerated per `npm run benchmark`)
tests/quality/results.json
tests/quality/scorer-results.json
tests/e2e/temp-prompt.txt

# Private rebaseline corpora; check in only sanitized manifests/summaries.
artifacts/rebaseline-*/generations.private.jsonl
artifacts/rebaseline-*/*.private.jsonl
artifacts/rebaseline-*/private/
.vercel

# local test samples / shareable bundles (user-owned, not repo fixtures)
sample/

# Demo render scratch outputs (only the curated patina-preview-<lang>.gif/png are tracked)
assets/demo/*real-tistory*
# Dev/QA scratch under artifacts/ — screenshots, browser runs, QA reports.
# Regenerated per QA pass, never committed. Curated corpora live in tracked
# subdirs (persona-calibration-*/, rebaseline-*/) and are unaffected by these.
artifacts/*-qa/
artifacts/demo-en/
artifacts/*.png
artifacts/*.jpg
artifacts/*.jpeg
artifacts/ai-test-sources.md

# Rewrite-efficacy study raw outputs (regenerated; text bodies stay private)
artifacts/rewrite-efficacy-pilot/
artifacts/rewrite-efficacy-study1/
artifacts/rewrite-efficacy-study2/
artifacts/rewrite-efficacy-study3/
artifacts/rewrite-efficacy-study4/

# Private model-study rewrites and judge rationales; publish sanitized rows only.
artifacts/model-evaluation-*/*.private.jsonl
artifacts/model-evaluation-*/**/*.private.jsonl
artifacts/model-evaluation-*/**/*.log
artifacts/model-evaluation-*/**/job.json
artifacts/model-evaluation-*/**/job.json.*.tmp
artifacts/model-evaluation-*/**/start.lock*
artifacts/model-evaluation-*/**/*.private.json*
artifacts/model-evaluation-*/**/.writer-*.lock
artifacts/model-evaluation-*/**/.writer.lock
# Invalidated pilot is retained locally; only validated measurements are published.
artifacts/model-evaluation-20260904/scorer-*/
artifacts/model-evaluation-20260904/smoke-gemini/
artifacts/model-evaluation-20260904/jobs/

# Judge-calibration side study raw outputs (same policy: raw text never committed)
artifacts/judge-calibration-2026/
.env*

# Local review-tool workspace (repo packs + model responses); never part of the tree
.insane-review/
